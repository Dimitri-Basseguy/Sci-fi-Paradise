{% extends 'base.html.twig' %}

{% block title %}Edit Book{% endblock %}

{% block body %}
<div class="container">
    <h1 class>Editer : "{{ book.title }}"</h1>
    <div class="container d-flex flex-column align-items-center justify-content-xl-center">
        <div class="bg-light border rounded" style="width: 500px;padding: 1rem;background-color: #dfe8ee;">  

            {{ include('book/_form.html.twig', {'button_label': 'Modifier'}) }}

            <a href="{{ path('book_index') }}" class="mt-4">Retour à la liste</a>

            {{ include('book/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
