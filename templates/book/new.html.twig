{% extends 'base.html.twig' %}

{% block title %}New Book{% endblock %}

{% block body %}
    <div class="container d-xl-flex justify-content-xl-center">
       
        <div style="width: 400px;">  
        {{ include('book/_form.html.twig') }}

        <a href="{{ path('book_index') }}">Retour à la liste</a>
        </div>
    </div>
{% endblock %}
